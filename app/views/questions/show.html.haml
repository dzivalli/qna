%h4= @question.title
%hr
%p= @question.body

%h4 Answers:
%ul.list-group
  - @question.answers.each do |answer|
    = render partial: 'answers/answer', locals: {answer: answer}

Your answer:
= render 'answers/form'




